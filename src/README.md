# Source

This folder will contain the `AzureDataLabToolkit` PowerShell module.

The planned source boundaries are:

```text
Core/
Providers/
Engines/
Public/
Private/
```

Executable scaffolding will be added after the architecture and initial SQL VM provider contract are approved.
